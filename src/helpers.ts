// export const addNums = (x: number, y: number) => x + y;
// export const subNums = (x: number, y: number) => x - y;
// export const multiplyNums = (x: number, y: number) => x * y;
// export const divideNums = (x: number, y: number) => x / y;

export class Math {
  adder = (x: number, y: number) => x + y;
  subtracter = (x: number, y: number) => x - y;
  divider = (x: number, y: number) => x / y;
  multiplier = (x: number, y: number) => x * y;
}
